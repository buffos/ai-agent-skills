# Canonical Use-Case Modeler

Defines the stable application-layer surface between the domain model and the
external contract.

Use it after the domain model is mature enough to name business intents
cleanly. It writes `docs/architecture/<capability-slug>/canonical-use-cases.md` with commands, queries,
application services, transaction expectations, idempotency-sensitive
operations, and failure categories.
