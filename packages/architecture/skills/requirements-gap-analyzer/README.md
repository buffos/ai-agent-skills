# Requirements Gap Analyzer

Finds the smallest set of high-value questions needed to unblock the next
artifact.

Use it when artifacts already exist but still contain contradictions, missing
rules, underspecified workflows, or unstable terminology. It writes
`docs/architecture/<capability-slug>/requirements-gap-analysis.md` and is the precision follow-up companion to
the broader discovery interviewer.
