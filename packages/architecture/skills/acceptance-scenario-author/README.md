# Acceptance Scenario Author

Writes stable acceptance scenarios from the PRD and canonical design artifacts.

Use it after the PRD, domain model, use-case model, and contract are strong
enough to define observable behavior clearly. It produces implementation-neutral
scenarios in `docs/architecture/<capability-slug>/acceptance-scenarios.md` that later tests and architecture
comparisons can reuse.
