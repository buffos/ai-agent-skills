# PRD Author

Turns structured discovery material into a concrete, architecture-neutral PRD.

Use it once discovery is strong enough to state the product problem, actors,
scope, workflows, business rules, constraints, and non-goals. It writes
`docs/architecture/<capability-slug>/prd.md` and establishes the product baseline that downstream design
artifacts build on.
