# Canonical Contract Author

Defines the stable external API and CLI contract from the PRD, domain model,
and use-case model.

Use it after commands, queries, statuses, and error semantics are already
stable. It writes `docs/architecture/<capability-slug>/canonical-api-cli-contract.md` and preserves external
behavior without leaking internal implementation details.
