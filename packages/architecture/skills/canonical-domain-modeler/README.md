# Canonical Domain Modeler

Builds the shared business model that all later implementations should preserve.

Use it after the PRD and glossary are strong enough to name concepts and rules
cleanly. It writes `docs/architecture/<capability-slug>/canonical-domain-model.md` with aggregates, entities,
value objects, invariants, lifecycle rules, policies, and domain events.
