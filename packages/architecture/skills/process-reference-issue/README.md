# Process Reference Issue

Implements one local issue from the reference-document issue system.

Use it after `reference-docs-to-issues` has already created grounded issue
files. It reads the issue plus its parent artifacts, implements the work,
verifies it, updates the issue registry, and escalates back to the upstream
architecture skills if it finds a real spec gap.
